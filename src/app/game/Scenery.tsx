"use client";

// Intentionally empty — sky level, no props on the sides.
type SceneryProps = {
  curve?: any;
  arcLength?: number;
  environment?: any;
  loopIdx?: number;
};

export default function Scenery(_props: SceneryProps = {}) {
  return null;
}
